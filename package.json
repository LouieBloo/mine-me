{
  "name": "nvg",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=24.12.0"
  },
  "packageManager": "npm@11.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "setup": "node -e 'if (!process.version.startsWith(\"v24\")) { console.error(\"ERROR: Node v24 is required. Please run \\\"nvm use\\\" to switch to the correct version.\"); process.exit(1); }' && npm install && npm run db:setup",
    "db:setup": "cd apps/server && npx prisma db push && npx prisma generate"
  },
  "devDependencies": {
    "turbo": "latest",
    "typescript": "^5.0.0"
  }
}